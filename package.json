{
  "name": "proyecto_ecommerce",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx @tailwindcss/cli -i ./src/style/input.css -o ./src/style/output.css --watch",
    "build:tailwind": "npx @tailwindcss/cli -i ./src/style/input.css -o ./src/style/output.css --minify",
    "build": "npm run build:tailwind && mkdir -p dist && cp -r ./src/* ./dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@tailwindcss/cli": "^4.1.16",
    "cors": "^2.8.5",
    "nodemon": "^3.1.10",
    "tailwindcss": "^4.1.16"
  },
  "devDependencies": {
    "@types/node": "^24.10.0"
  }
}
